class ComputerPlayer extends Player {

  /*private Piece[][] board;
  private boolean isWhite;
  ArrayList<int[]> pieces;

  //otherwise known as WHO CARES ABOUT EFFICIENCY HAHAHAAA
  //i think i'm going to cry
  public ComputerPlayer(Board hat, boolean isWhite) {
    Piece[][] b = hat.getBoard();
    for (int i = 0; i < 8; i++) {
      for (int j = 0; j < 8; j++) {
        this.board[i][j] = b[i][j] == null ? 
          null :
          new Piece(b[i][j].getType(),b[i][j].isWhite(), b[i][j].getMoves());
      }
    }
    this.isWhite = isWhite;
  }

 public int[][] getMove() {
    return easy();
  }

  public int[][] easy() {
    int[] location = pieces.get((int)Math.random()*8);
    while (board)
  }

  private ArrayList<int[]> getPieces(boolean white) {  
    ArrayList<int[]> pieceLocations = new ArrayList<int[]>();
    for (int i = 0; i < board.length(); i++) {
      for (int j = 0; j < board.length(); j++) {
        if (board[i][j] != null && board[i][j].isWhite() == white) piecesLocations.add(new int[] {i, j});
      }
    }
  }*/

}